# Core data handling
pandas==2.3.1

# Web UI
streamlit==1.48.0

# Load environment variables from .env
python-dotenv==1.1.1

# LangChain (core + OpenAI provider) – version 0.3.x
langchain==0.3.5
langchain-openai==0.3.19

# The experimental sub‑package that holds the pandas‑agent
langchain-experimental==0.3.4

# (optional) pretty‑printing of markdown tables in the logs
tabulate==0.9.0
