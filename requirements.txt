# Core data handling
pandas>=2.0.0

# Web UI
streamlit>=1.28.0

# Load environment variables from .env
python-dotenv>=1.0.0

# LangChain (core + OpenAI provider) – latest stable versions
langchain>=0.1.0
requests>=2.31.0
langchain-experimental>=0.1.0

# (optional) pretty‑printing of markdown tables in the logs
tabulate>=0.9.0
